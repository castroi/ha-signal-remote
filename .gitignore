node_modules/
dist/
*.log
.env
.env.*
!.env.example
!dev/.env.dev.example
coverage/
.DS_Store

# Local harness / editor state
.claude/

# Live per-deployment alias table (derive from config/aliases.example.yaml)
config/aliases.yaml

# Dev stack: real env file + Home Assistant runtime state (keep only the config)
.env.dev
dev/homeassistant/*
!dev/homeassistant/configuration.yaml

# Local deployment manifests — kept on the deploy host, not versioned here
deploy/
